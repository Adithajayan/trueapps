[phases.setup]
aptPkgs = [
  "libcairo2",
  "libpango-1.0-0",
  "libpangocairo-1.0-0",
  "libgdk-pixbuf2.0-0",
  "shared-mime-info",
  "libffi-dev"
]

[phases.install]
cmds = [
  "pip install -r requirements.txt"
]

[start]
cmd = "bash start.sh"